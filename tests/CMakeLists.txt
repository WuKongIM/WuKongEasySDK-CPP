add_executable(wukong_unit unit.cpp)
target_include_directories(wukong_unit PRIVATE ../src)
target_link_libraries(wukong_unit PRIVATE WuKongEasySDK::WuKongEasySDK)
add_test(NAME unit COMMAND wukong_unit)
add_executable(wukong_integration_client integration_client.cpp)
target_link_libraries(wukong_integration_client PRIVATE WuKongEasySDK::WuKongEasySDK Threads::Threads)
option(WUKONG_INTEGRATION_TESTS "Register Python WS/WSS integration tests (requires websockets)" OFF)
if(WUKONG_INTEGRATION_TESTS)
    find_package(Python3 REQUIRED COMPONENTS Interpreter)
    add_test(NAME websocket_integration COMMAND ${Python3_EXECUTABLE}
        ${CMAKE_CURRENT_SOURCE_DIR}/integration.py --client $<TARGET_FILE:wukong_integration_client>)
    set_tests_properties(websocket_integration PROPERTIES LABELS integration TIMEOUT 120)
endif()
add_executable(wukong_product_smoke product_smoke.cpp)
target_link_libraries(wukong_product_smoke PRIVATE WuKongEasySDK::WuKongEasySDK Threads::Threads)
