cmake_minimum_required(VERSION 3.20)
project(WuKongVcpkgConsumer LANGUAGES CXX)
find_package(WuKongEasySDK 0.1 CONFIG REQUIRED)
add_executable(wukong_vcpkg_consumer main.cpp)
target_link_libraries(wukong_vcpkg_consumer PRIVATE WuKongEasySDK::WuKongEasySDK)
enable_testing()
add_test(NAME installed_sdk_lifecycle COMMAND wukong_vcpkg_consumer)
set_tests_properties(installed_sdk_lifecycle PROPERTIES TIMEOUT 10)
